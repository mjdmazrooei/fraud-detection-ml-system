[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fraudsys"
version = "0.1.0"
description = "Real-time fraud detection ML system (end-to-end)"
requires-python = ">=3.10"
